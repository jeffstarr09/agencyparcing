service_account.json
*.pyc
__pycache__/
.env
.venv/

# Run output. Rebuilt every run; never belongs in the repo.
results*.csv
agencies*.csv
clients.csv
agency_tiktok.csv
agency_scorecard.csv
agency_source_failures.csv
feedback_report.md
review.csv
feedback_verdicts.csv

# Where you are in a run, and your app preferences. Personal, not shared.
data/run_state.json
data/app_settings.json

# Fetched pages. Stops re-runs from re-hitting anyone's site; gets large.
.cache/

# NOT ignored, and deliberately so:
#   data/learned_rules.json   what your corrections taught the parser
#   data/source_health.json   which directories have gone quiet
# Both are state worth keeping and worth seeing in a diff.
